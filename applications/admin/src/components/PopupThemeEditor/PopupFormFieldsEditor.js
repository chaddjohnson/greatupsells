import React from 'react';
import PropTypes from 'prop-types';

const PopupFormFieldsEditor = ({ popupTheme, onChange }) => {
  return <>Form fields editor</>;
};

PopupFormFieldsEditor.propTypes = {
  //
};

export default PopupFormFieldsEditor;
