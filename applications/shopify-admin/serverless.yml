service: upselling-shopify-admin

provider:
  name: aws
  runtime: nodejs12.x
  memorySize: 256
  timeout: 10
  endpointType: REGIONAL
  stage: ${opt:stage, 'dev'}
  region: ${opt:region, 'us-east-1'}
  versionFunctions: false
  logRetentionInDays: 14
  environment:
    API_URL: ${ssm:/upselling/${self:provider.stage}/api/url}
    MONGODB_URI: ${ssm:/upselling/${self:provider.stage}/database/mongodb/uri~true}
    SHOPIFY_ADMIN_URL: ${ssm:/upselling/${self:provider.stage}/shopify-admin/url}
    SHOPIFY_API_KEY: ${ssm:/upselling/${self:provider.stage}/shopify-admin/api-key}
    SHOP_COLLECTION_IMPORT_QUEUE_URL: ${ssm:/upselling/${self:provider.stage}/shop-collection-import-queue-url}
    SHOP_PRODUCT_IMPORT_QUEUE_URL: ${ssm:/upselling/${self:provider.stage}/shop-product-import-queue-url}
    WEBHOOKS_API_URL: ${ssm:/upselling/${self:provider.stage}/webhooks-api/url}

functions:
  health:
    memorySize: 128
    handler: server/health.handler
    events:
      - http:
          path: /health
          method: get
  region:
    memorySize: 128
    handler: server/region.handler
    events:
      - http:
          path: /region
          method: get
  server:
    handler: server/index.handler
    events:
      - http:
          path: /
          method: ANY
      - http:
          path: /{any+}
          method: ANY

plugins:
  - serverless-plugin-monorepo
  - serverless-domain-manager

custom:
  customDomain:
    domainName: ${ssm:/upselling/${self:provider.stage}/shopify-admin/endpoint/${self:provider.region}}
    basePath: ''
    certificateName: ${ssm:/upselling/${self:provider.stage}/certificate-name}
    createRoute53Record: true
    endpointType: regional
    securityPolicy: tls_1_2

package:
  exclude:
    - test/**
    - node_modules/aws-sdk
    - node_modules/**/aws-sdk
    - node_modules/**/*.map
