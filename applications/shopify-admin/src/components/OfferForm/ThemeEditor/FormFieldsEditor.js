import React from 'react';
import PropTypes from 'prop-types';

const FormFieldsEditor = ({ fields }) => <>Form fields editor here</>;

FormFieldsEditor.propTypes = {
  fields: PropTypes.array.isRequired
};

export default FormFieldsEditor;
