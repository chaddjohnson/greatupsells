import React from 'react';
import PropTypes from 'prop-types';

const CodeEditor = ({ markup }) => <>Code editor here</>;

CodeEditor.propTypes = {
  markup: PropTypes.string.isRequired
};

export default CodeEditor;
