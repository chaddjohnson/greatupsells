service: greatupsells-email-service
frameworkVersion: '^3.38'
useDotenv: true
configValidationMode: error
provider:
  name: aws
  architecture: arm64
  runtime: nodejs20.x
  timeout: 10
  endpointType: REGIONAL
  stage: ${opt:stage, 'dev'}
  region: ${opt:region, 'us-east-1'}
  versionFunctions: false
  logRetentionInDays: 14
  httpApi:
    shouldStartNameWithService: true
    cors: true
  environment:
    NODE_ENV: ${env:NODE_ENV, 'production'}
    LOG_QUEUE_URL: ${ssm:/greatupsells/${self:provider.stage}/queues/log/url, ''}
    GSUITE_APP_PASSWORD: ${env:GSUITE_APP_PASSWORD, ssm:/greatupsells/${self:provider.stage}/email-service/gsuite-app-password}

custom:
  customDomain:
    domainName: ${ssm:/greatupsells/${self:provider.stage}/email-service/regional-domain, ''}
    basePath: ''
    certificateName: ${ssm:/greatupsells/${self:provider.stage}/certificate-name, ''}
    createRoute53Record: true
    endpointType: regional
    apiType: http
    route53Params:
      healthCheckId: ${ssm:/greatupsells/${self:provider.stage}/email-service/health-check-id, ''}
    securityPolicy: tls_1_2
    autoDomain: true

functions:
  health:
    handler: src/routes/health.handler
    events:
      - httpApi:
          path: /health
          method: get
  test:
    handler: src/routes/test.handler
    events:
      - httpApi:
          path: /test
          method: get
    environment:
      EMAIL_QUEUE_URL: ${ssm:/greatupsells/${self:provider.stage}/queues/email-sending/url, ''}
  sending:
    role: ${env:SERVICES_CONSUMER_ROLE_ARN, ssm:/greatupsells/${self:provider.stage}/roles/services-consumer-arn}
    memorySize: 256
    handler: src/workers/sending.handler
    events:
      - sqs:
          arn: ${ssm:/greatupsells/${self:provider.stage}/queues/email-sending/arn, 'arn:aws:sqs:${self:provider.region}:000000000000:email-sending-queue-dev'}
          batchSize: 1
          maximumConcurrency: 3

plugins:
  - serverless-plugin-monorepo
  - serverless-domain-manager

package:
  patterns:
    - '!infrastructure/**'
    - '!node_modules/**/*.map'
    - '!node_modules/eslint'
